[project]
name = "argus-shared"
version = "0.1.0"
description = "Shared Pydantic models, SQLAlchemy DB session, and Celery queue primitives"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
