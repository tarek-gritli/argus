[project]
name = "argus-cli"
version = "0.1.0"
description = "Typer CLI for local pre-commit review, fix application, and watch mode"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
cli = "main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
